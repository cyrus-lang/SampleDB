import std::libc{printf};
import engine;
import utils;

func main() {
	#table: engine::Table* = engine::Table.create("my_table", null, 0);
	if (!table) {
		utils::panic("failed to create table.");
	} 

	table->free();
}